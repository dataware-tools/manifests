apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: app-launcher
  name: app-launcher
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-launcher
  strategy: {}
  template:
    metadata:
      labels:
        app: app-launcher
        require-auth0: disabled
    spec:
      containers:
      - image: registry.gitlab.com/dataware-tools/app-launcher:v0.2.0
        name: app
        ports:
          - containerPort: 3000
        imagePullPolicy: Always
        livenessProbe:
          httpGet:
            path: /healthz
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 10
          failureThreshold: 3
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: app-launcher
spec:
  selector:
    app: app-launcher
  ports:
  - name: http
    protocol: TCP
    port: 3000
    targetPort: 3000
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: dataware-tools-app-launcher
spec:
  hosts:
    - "*"
  gateways:
    - dataware-tools-gateway
  http:
    - match:
        - uri:
            prefix: /launcher
      rewrite:
        uri: /
      route:
        - destination:
            host: app-launcher
