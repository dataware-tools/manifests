apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: api-content-store
    require-data-mounts: enabled
    require-pydtk-v3: enabled
  name: api-content-store
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-content-store
  strategy: {}
  template:
    metadata:
      labels:
        app: api-content-store
        version: v1
        require-auth0: enabled
    spec:
      containers:
      - image: registry.gitlab.com/dataware-tools/api-content-store:v1.0
        name: app
        ports:
          - containerPort: 8080
        env:
          - name: PORT
            value: "8080"
          - name: SECRET_KEY
            value: "api-content-store"
        imagePullPolicy: Always
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
          failureThreshold: 3
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: api-content-store
spec:
  selector:
    app: api-content-store
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: 8080
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: dataware-tools-api-content-store
spec:
  hosts:
    - "*"
  gateways:
    - dataware-tools-gateway
  http:
    - name: api-content-store
      match:
        - uri:
            prefix: /api/latest/content_store/
      rewrite:
        uri: /
      route:
        - destination:
            host: api-content-store